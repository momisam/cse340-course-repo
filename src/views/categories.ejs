<%- include('partials/header') %>

<main>
        <h1><%= title %></h1>
        <h2>Categories</h2>
        <ul>
            <li>
                <% category.forEach(category => { %>
                    <strong><%= category.name %></strong>
                <% }); %>
            </li>
            
        </ul>
    </main>
<%- include('partials/footer') %>